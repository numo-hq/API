type Factory @entity {
  "factory address"
  id: ID!
  "amount of lendgines created"
  lendgineCount: BigInt!
}

type Lendgine @entity {
  "Address"
  id: ID!

  "immutables"
  token0: Bytes!
  token1: Bytes!
  token0Exp: Int!
  token1Exp: Int!
  upperBound: BigInt!
}
